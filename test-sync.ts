import { Editor } from '@tiptap/core';
import StarterKit from '@tiptap/starter-kit';
import { AnnotationMark } from './src/components/editor/extensions/AnnotationMark.js'; // Assuming we can import it, wait, we can't easily run TS in node without ts-node.
